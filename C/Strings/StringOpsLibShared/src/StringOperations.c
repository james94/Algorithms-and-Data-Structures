#include "StringOperations.h"

#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

void FindLength(char *s)
{
    int i;
    for(i = 0; s[i] != '\0'; i++)
    {

    }
    printf("Length is %d\n", i);
}